# Copyright 2025 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Create check
description: Creates a check on a commit.

inputs:
  commit-sha:
    description: The commit SHA to create the check for.
    required: true
  name:
    description: The name of the check.
    required: true
  status:
    description: The status of the check, one of "queued", "in_progress", "completed".
    default: queued
  conclusion:
    description: The conclusion of the check, one of "action_required", "cancelled", "failure", "neutral", "success", "skipped", "timed_out".
  details-url:
    description: The URL to the details of the check.
  title:
    description: The title of the check, this appears in the "progress" of the check UI.
  summary:
    description: The summary of the check.
  text:
    description: The text of the check.
  repository-owner:
    description: The owner of the repository.
    default: ${{ github.repository_owner }}
  repository-name:
    description: The name of the repository.
    default: ${{ github.event.repository.name }}

outputs:
  run-id:
    description: The run ID of the check.
    value: ${{ fromJson(steps.check.outputs.result).id }}

runs:
  using: composite
  steps:
    - uses: actions/github-script@v7
      id: check
      with:
        script: |
          // https://docs.github.com/en/rest/checks/runs?apiVersion=2022-11-28#create-a-check-run
          // https://octokit.github.io/rest.js/v21/#checks-create
          const detailsUrl = `${{ inputs.details-url }}` || undefined;
          if (detailsUrl) {
            core.warning(`details-url is not supported unless in a GitHub app.`);
          }
          const title = `${{ inputs.title }}` || undefined;
          const summary = `${{ inputs.summary }}`;
          const text = `${{ inputs.text }}` || undefined;
          let output = undefined;
          if (title || summary || text) {
            output = {title: title, summary: summary, text: text};
          }
          const check = await github.rest.checks.create({
            owner: "${{ inputs.repository-owner }}",
            repo: "${{ inputs.repository-name }}",
            head_sha: "${{ inputs.commit-sha }}",
            name: `${{ inputs.name }}` || undefined,
            status: "${{ inputs.status }}",
            conclusion: "${{ inputs.conclusion }}" || undefined,
            details_url: detailsUrl,
            output: output,
          });
          return check.data;
