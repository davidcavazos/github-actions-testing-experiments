name: on_pull_request

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled

jobs:
  allow:
    name: Allowed
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Running job with full permissions"

