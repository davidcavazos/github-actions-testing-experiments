name: on_pull_request

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled

jobs:
  allow:
    name: Authorizing, please wait for tests to run
    runs-on: ubuntu-latest
    steps:
      - run: echo "🚨 Skipping check and running job with full permissions"
