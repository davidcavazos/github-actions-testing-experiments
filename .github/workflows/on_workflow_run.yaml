name: Test

on:
  push:
    branches:
      - main
  workflow_run:
    workflows:
      - authorize
    types:
      - completed
  workflow_dispatch: # Manual runs

jobs:
  test:
    name: Run tests ${{ github.head_ref || '<main>' }}
    runs-on: ubuntu-latest
    permissions:
      checks: write  # Permission to create a Check Run
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - run: echo "🚨 Malicious unauthorized code with full permissions 🚨"
